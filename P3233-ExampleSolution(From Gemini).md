


这道题是非常经典的**虚树（Virtual Tree）**结合**树形 DP** 和**倍增（Binary Lifting）**的综合性题目。

题目最核心的特征是：一棵大树上有多次询问，每次询问给定一部分“关键点”（议事处），且所有询问的关键点总数 $\sum m_i \le 300000$。这种“总体规模受限”的特征是使用**虚树**的强烈信号。

下面是这道题的详细解题思路：

### 核心思想：虚树 + 树上倍增

由于每次询问的点数不多，如果每次都在原树上做 DFS 寻找归属，时间复杂度会爆炸。因此我们需要针对每次询问，把被选中的临时议事处以及它们两两之间的**最近公共祖先（LCA）**提取出来，重新连边构成一棵“虚树”。虚树的节点数最多是 $2m_i-1$ 个。
我们在虚树上计算每个节点受哪个议事处管辖，然后再根据虚树上的边，把原树中被“隐藏”的节点数量分配给对应的议事处。

---

### 第一步：原树预处理
在开始处理询问前，先对原树进行一次彻底的 DFS，预处理出以下信息：
1. **节点深度** $depth[u]$ 和 **子树大小** $size[u]$。
2. **DFS 序** $dfn[u]$（用于构建虚树时的节点排序）。
3. **倍增数组** $fa[u][i]$：表示节点 $u$ 往上跳 $2^i$ 步到达的祖先，用于快速求 LCA 以及后续找路径分割点。

---

### 第二步：构建虚树
对于每一年的询问（给定 $m$ 个议事处）：
1. **强制加入根节点**：为了方便最后统计，如果原树的根节点（节点 1）不在给定的议事处中，我们也把它强制加进来（但标记它不是议事处）。这样虚树的根永远是节点 1。
2. **排序**：将这 $m$ 个点（加上节点 1）按照 $dfn$ 排序。
3. **单调栈建树**：遍历排序后的节点，利用单调栈求相邻节点的 LCA，并将节点和 LCA 连边，构建出虚树。

---

### 第三步：两次 DFS 确定虚树节点的归属 (树形 DP)
虚树建好后，我们需要知道虚树上的每一个节点 $u$ 究竟被哪个议事处管辖（设管辖它的议事处为 $belong[u]$）。这可以通过两次 DFS 完成：

1. **第一次 DFS（自底向上）**：
   从叶子向根更新。对于节点 $u$，它的 $belong[u]$ 可能来自于它在虚树上的子节点 $v$。
   - 比较 $u$ 到 $belong[v]$ 的距离与 $u$ 当前的最优距离。
   - 如果距离更近，或者距离一样但 $belong[v]$ 编号更小，就更新 $belong[u]$。

2. **第二次 DFS（自顶向下）**：
   从根向叶子更新。对于节点 $v$，它的 $belong[v]$ 可能来自于它在虚树上的父节点 $u$。
   - 比较 $v$ 到 $belong[u]$ 的距离与 $v$ 当前的最优距离，同样考虑距离和编号的优先级进行更新。

这两次 DFS 结束后，**虚树上的每一个节点**都已经找到了绝对正确的 $belong$。

---

### 第四步：将原树的节点“分配”给议事处（核心难点）
虚树上的节点只是原树的冰山一角。原树中还有大量的节点没有在虚树中出现，我们需要把它们的数量加到对应的 $belong$ 答案中。

对于虚树上的每个节点 $u$，我们初始令其剩余管辖范围 `rem_size[u] = size[u]`（即它在原树上的整棵子树大小）。

接下来，我们遍历虚树上的每一条边（设虚树边为 $u \to v$，$u$ 是 $v$ 的祖先）：
这条虚树边其实代表了原树上一条从 $u$ 到 $v$ 的路径。我们需要处理这条路径上“分叉”出去的子树：
1. **找到原树上的直接分支**：利用倍增，找到 $v$ 的祖先中，正好在 $u$ 下面一层的节点，记为 $w$。
2. **剥离 $w$ 的子树**：因为 $w$ 所在的这一整条分支属于虚树边 $u \to v$ 的探讨范围，所以我们要把 $size[w]$ 从 $u$ 的初始势力范围中减去：`rem_size[u] -= size[w]`。
3. **分配 $w$ 到 $v$ 路径上的点**：路径上的节点（以及它们挂着的子树）总数为 $size[w] - size[v]$。这部分点该归属 $belong[u]$ 还是 $belong[v]$？
   - **情况 A**：如果 $belong[u] == belong[v]$，那太好了，这段路径上所有的点都归属 $belong[u]$，直接把答案加上 $size[w] - size[v]$。
   - **情况 B**：如果 $belong[u] \neq belong[v]$，说明这条路径上存在一个**分界点**（临界点）。
     - 分界点以上的节点离 $u$ 更近，归属 $belong[u]$。
     - 分界点以下的节点离 $v$ 更近，归属 $belong[v]$。
     - 我们可以在 $v$ 到 $w$ 的路径上使用**树上倍增**，根据距离公式和编号大小规则，二分跳跃找到这个**最高的分界点 $mid$**（即正好归属 $belong[v]$ 的最浅节点）。
     - 找到 $mid$ 后，将 $size[w] - size[mid]$ 分配给 $belong[u]$；将 $size[mid] - size[v]$ 分配给 $belong[v]$。

最后，把虚树上所有节点经过上述扣减后剩下的 `rem_size[u]`，全部加到各自的 `belong[u]` 的答案中即可。

---

### 第五步：输出与清空
- 将答案按照该年询问给定的议事处顺序输出。
- **注意清空虚树**：为了保证 $O(\sum m_i)$ 的时间复杂度，**绝不能用 `memset` 清空全部数组**。必须通过遍历刚才建好的虚树节点，把用到的点的边表、标记等重置为 0。

### 总结
这道题的逻辑链条非常清晰：**提取关键点建虚树 $\to$ 两次 DP 确定关键点的势力归属 $\to$ 利用倍增切分原树的边进行势力结算**。把握好这三个阶段，理清节点深度、原树 $size$ 之间的加减关系，就能顺利解决此题。